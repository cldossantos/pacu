% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics_api.R
\name{pa_get_vi_stats}
\alias{pa_get_vi_stats}
\title{Request vegetation index statistics from the Data Space Statistics API}
\usage{
pa_get_vi_stats(
  aoi,
  start.date,
  end.date,
  collection = c("sentinel-2-l2a"),
  vegetation.index = c("bsi", "evi", "gcvi", "ndre", "ndvi", "reci"),
  agg.time = c("P1D", "P5D", "P10D"),
  by.feature = FALSE
)
}
\arguments{
\item{aoi}{`sf` object used to define the area of interest.}

\item{start.date}{beginning of the time window used to filter satellite
products. Date format should be `\%Y-\%m-\%d`.}

\item{end.date}{end of the time window used to filter satellite products.
Date format should be `\%Y-\%m-\%d`.}

\item{collection}{satellite collection. Currently only `sentinel-2-l2a` is
supported.}

\item{vegetation.index}{vegetation index to request from Data Space.}

\item{agg.time}{aggregation interval for the statistics request.}

\item{by.feature}{logical; whether statistics should be retrieved separately
for each polygon when multiple polygons are supplied in `aoi`.}
}
\value{
An object of class `veg.index` and `stars`.
}
\description{
Request vegetation index statistics from the Data Space
  Statistics API.
}
\details{
`pa_get_vi_stats()` communicates with the Data Space Statistics API
  through HTTP requests and returns area-level vegetation index summaries for
  the requested area and time range.
}
\examples{
\dontrun{
extd.dir <- system.file("extdata", package = "pacu")
area.of.interest <- sf::st_read(file.path(extd.dir, 'cobs_a_aoi.shp'), quiet = TRUE)
ndvi <- pa_get_vi_stats(aoi = area.of.interest,
                        start.date = '2021-01-01',
                        end.date = '2021-12-31',
                        vegetation.index = 'ndvi')
}

}
\author{
Caio dos Santos and Fernando Miguez
}
